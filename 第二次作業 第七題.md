# 題目與求解過程（傅立葉級數）

## 1. 題目設定

### 函數與區間

$$
f(x) = x + \pi
$$

$$
x \in [-\pi, \pi]
$$

### 求解目標
* 求函數 $f(x)$ 之傅立葉級數（Fourier series）

---

## 2. 求解過程

### 2.1 傅立葉級數公式
對於週期 $T = 2\pi$（半週期 $L = \pi$）的函數，其傅立葉級數展開式為：

$$
f(x) = \frac{a_0}{2} + \sum_{n=1}^{\infty} [a_n \cos(nx) + b_n \sin(nx)]
$$

### 2.2 計算直流係數 $a_0$

$$
a_0 = \frac{1}{\pi} \int_{-\pi}^{\pi} (x + \pi) \, dx
$$

$$
a_0 = \frac{1}{\pi} \int_{-\pi}^{\pi} x \, dx + \frac{1}{\pi} \int_{-\pi}^{\pi} \pi \, dx
$$

由於 $x$ 為奇函數，在對稱區間 $[-\pi, \pi]$ 的積分為 0：

$$
a_0 = 0 + \frac{1}{\pi} \Big[ \pi x \Big]_{-\pi}^{\pi}
$$

$$
a_0 = \frac{1}{\pi} [ \pi^2 - (-\pi^2) ]
$$

$$
a_0 = \frac{2\pi^2}{\pi}
$$

$$
a_0 = 2\pi
$$

因此直流分量為：

$$
\frac{a_0}{2} = \pi
$$

### 2.3 計算餘弦係數 $a_n$ ($n \ge 1$)

$$
a_n = \frac{1}{\pi} \int_{-\pi}^{\pi} (x + \pi) \cos(nx) \, dx
$$

$$
a_n = \frac{1}{\pi} \int_{-\pi}^{\pi} x \cos(nx) \, dx + \frac{1}{\pi} \int_{-\pi}^{\pi} \pi \cos(nx) \, dx
$$

由於 $x \cos(nx)$ 為奇函數，在對稱區間的積分為 0：

$$
a_n = 0 + \int_{-\pi}^{\pi} \cos(nx) \, dx
$$

$$
a_n = \Big[ \frac{\sin(nx)}{n} \Big]_{-\pi}^{\pi}
$$

$$
a_n = \frac{\sin(n\pi) - \sin(-n\pi)}{n}
$$

由於 $n$ 為正整數：

$$
\sin(n\pi) = 0
$$

因此餘弦係數為：

$$
a_n = 0
$$

### 2.4 計算正弦係數 $b_n$ ($n \ge 1$)

$$
b_n = \frac{1}{\pi} \int_{-\pi}^{\pi} (x + \pi) \sin(nx) \, dx
$$

$$
b_n = \frac{1}{\pi} \int_{-\pi}^{\pi} x \sin(nx) \, dx + \frac{1}{\pi} \int_{-\pi}^{\pi} \pi \sin(nx) \, dx
$$

由於 $\pi \sin(nx)$ 為奇函數，在對稱區間的積分為 0；而 $x \sin(nx)$ 為偶函數：

$$
b_n = \frac{2}{\pi} \int_{0}^{\pi} x \sin(nx) \, dx + 0
$$

使用分部積分法，令：

$$
u = x \implies du = dx
$$

$$
dv = \sin(nx)dx \implies v = -\frac{\cos(nx)}{n}
$$

代入分部積分公式：

$$
b_n = \frac{2}{\pi} \left( \Big[ -\frac{x \cos(nx)}{n} \Big]_{0}^{\pi} - \int_{0}^{\pi} \left( -\frac{\cos(nx)}{n} \right) \, dx \right)
$$

$$
b_n = \frac{2}{\pi} \left( -\frac{\pi \cos(n\pi)}{n} + 0 + \frac{1}{n} \Big[ \frac{\sin(nx)}{n} \Big]_{0}^{\pi} \right)
$$

由於：

$$
\sin(n\pi) = 0
$$

$$
\sin(0) = 0
$$

因此化簡得到：

$$
b_n = \frac{2}{\pi} \left( -\frac{\pi \cos(n\pi)}{n} \right)
$$

$$
b_n = -\frac{2}{n} \cos(n\pi)
$$

代入：

$$
\cos(n\pi) = (-1)^n
$$

計算結果為：

$$
b_n = -\frac{2}{n} (-1)^n
$$

$$
b_n = \frac{2}{n} (-1)^{n+1}
$$

### 2.5 最終傅立葉級數展開式
將所有係數代回公式中：

$$
f(x) = \pi + \sum_{n=1}^{\infty} \frac{2}{n} (-1)^{n+1} \sin(nx)
$$

展開前幾項得：

$$
f(x) = \pi + 2 \sin(x) - \sin(2x) + \frac{2}{3} \sin(3x) - \frac{1}{2} \sin(4x) + \dots
$$

